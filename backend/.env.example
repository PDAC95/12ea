# ==================================
# ENVIRONMENT VARIABLES TEMPLATE
# Entre Amigas Backend API
# ==================================
# INSTRUCTIONS:
# 1. Copy this file to .env in the same directory
# 2. Fill in your actual values (remove placeholder text)
# 3. NEVER commit .env to version control!
# ==================================

# ----------------------------------
# SERVER CONFIGURATION
# ----------------------------------
# Environment mode: development | production | test
NODE_ENV=development

# Server port (default: 8000)
PORT=8000

# ----------------------------------
# DATABASE - MongoDB Atlas
# ----------------------------------
# MongoDB connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# Get from: MongoDB Atlas Dashboard > Connect > Connect your application
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# ----------------------------------
# JWT AUTHENTICATION
# ----------------------------------
# Secret key for JWT signing (MUST be at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_secreto_minimo_32_caracteres_requerido

# JWT token expiration time (examples: 7d, 24h, 1h, 30m)
JWT_EXPIRE=7d

# ----------------------------------
# EMAIL SERVICE - Resend
# ----------------------------------
# Resend API Key
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_tu_api_key_aqui

# Email sender address (must be verified domain in Resend)
EMAIL_FROM=noreply@ac95.ca

# Reply-to email address
EMAIL_REPLY_TO=hola@ac95.ca

# ----------------------------------
# AWS S3 - File Storage
# ----------------------------------
# AWS IAM Access Key ID
# Get from: AWS Console > IAM > Users > Security Credentials
AWS_ACCESS_KEY_ID=tu_access_key_id

# AWS IAM Secret Access Key
AWS_SECRET_ACCESS_KEY=tu_secret_access_key

# AWS Region where S3 bucket is located (example: us-east-1, eu-west-1)
AWS_REGION=us-east-1

# S3 Bucket name for file uploads (business logos, event images, etc.)
AWS_S3_BUCKET_NAME=entre-amigas-dev

# ----------------------------------
# FRONTEND CONFIGURATION
# ----------------------------------
# Frontend URL for CORS configuration
# Development: http://localhost:8080
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:8080

# CORS allowed origin (can be same as FRONTEND_URL)
CORS_ORIGIN=http://localhost:8080

# ----------------------------------
# RATE LIMITING
# ----------------------------------
# Time window for rate limiting in milliseconds (default: 15 minutes = 900000ms)
RATE_LIMIT_WINDOW_MS=900000

# Maximum number of requests per window per IP (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# ----------------------------------
# GOOGLE OAUTH 2.0 (Optional)
# ----------------------------------
# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxx

# Google OAuth Callback URL
# Development: http://localhost:8000/api/v1/auth/google/callback
# Production: https://yourdomain.com/api/v1/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:8000/api/v1/auth/google/callback
